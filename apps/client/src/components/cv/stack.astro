---
type Props = {
	id?: string;
	class?: string;
	style?: {
		gridColumn: string | number;
		gridRow: string | number;
	};
	type?: "column" | "line";
	alignItems?: "end";
	justifyContent?: "spaceBetween";
};

let {
	id,
	class: className = [],
	style,
	type = "column",
	alignItems,
	justifyContent,
} = Astro.props;
---

<div
	id={id}
	class:list={[
		"d-flex",
		type,
		className,
		{
			"f-column": type === "column",
			"f-end": alignItems === "end",
			"f-space-between": justifyContent === "spaceBetween",
		},
	]}
	style={style}
>
	<slot />
</div>

<style>
	.d-flex {
		display: flex;
	}

	.f-column {
		flex-direction: column;
	}

	.f-end {
		align-items: flex-end;
	}

	.f-space-between {
		justify-content: space-between;
	}
</style>
