---
import GitHub from "./icons/github.svg";
import LinkedIn from "./icons/linkedin.svg";

type TIconName = "github" | "linkedIn";

const EIcon = {
	github: GitHub,
	linkedIn: LinkedIn,
} as const;

type Props = {
	name: TIconName;
};

let { name } = Astro.props;

let TargetIcon = EIcon[name];
---

<TargetIcon width="100%" height="100%" />
