name: CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      DEPLOY_SRC: ${{secrets.DEPLOY_SRC}}
      DEPLOY_DEST: ${{secrets.DEPLOY_DEST}}

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
          cache: 'npm'

      - run: set -eu
      - run: mkdir "$HOME/.ssh"
      - run: echo "${{secrets.SSH_KEY}}" > "$HOME/.ssh/id_rsa"
      - run: chmod 600 "$HOME/.ssh/id_rsa"

      - run: npm ci
      - run: npm run build

      - run: sh scripts/deploy/deploy.sh
